# 结项代码任务清单

本文档承接 [中期清单](MID_TERM_CHECKLIST.md)。中期主链路和结项代码项现已完成；外部提交信息见 [结项交付说明](FINAL_SUBMISSION.md)。

## 一、验收口径

- `P0`：结项必须完成；
- `P1`：完整演示和资源生命周期所需；
- `P2`：原计划加分项，本版本也已完成。

## 二、完成清单

### 模块 A：性能评测（P0）

| 完成 | 任务 | 验收 |
| --- | --- | --- |
| [x] | Flat ground truth、Recall@K、平均查询耗时、构建耗时、索引字节 | Flat 自比为 1；K 超界使用有效分母 |
| [x] | `POST /api/eval` 多索引评测 | 严格参数校验、资源上界、结果快照 |
| [x] | 前端评测表和柱状图 | 多索引数值与 API 一致 |
| [x] | 评测回归测试 | Flat/IVF/HNSW/PQ、L2/Cosine 和边界覆盖 |

### 模块 B：用户、权限与运行安全（P0/P1）

| 完成 | 任务 | 验收 |
| --- | --- | --- |
| [x] | SQLAlchemy/SQLite 与 `User` 模型 | 自动建表、密码哈希、角色字段 |
| [x] | 注册、登录、JWT、`/auth/me` | 无效/过期/已删除用户 token 返回 401 |
| [x] | 管理员用户管理 | 普通用户 403；管理员不能自删 |
| [x] | 全业务 API 登录门禁 | 未登录查询、评测、数据、索引和历史均 401 |
| [x] | 前端登录门禁和统一 API 客户端 | token、multipart、非 JSON 错误和 401 处理 |
| [x] | 生产配置保护、CORS、JSON 错误、轮转日志 | 默认生产密钥/密码和 DEBUG 被拒绝 |

### 模块 C：多数据集与索引动态管理（P0/P1）

| 完成 | 任务 | 验收 |
| --- | --- | --- |
| [x] | `.h5ad/.npy/.csv` 上传与安全导入 | 临时文件、格式/有限数/维度校验、失败无残留 |
| [x] | 数据集元信息、指纹、列表、切换、重启恢复 | 多数据集独立切换；外部修改被拒绝 |
| [x] | 数据集删除 | 仅管理员；活动保护；关联索引级联清理 |
| [x] | 索引文件和 JSON 清单 | 数据集/文件指纹、算法参数、规模、版本完整记录 |
| [x] | `save/list/load/delete` 索引 API | 所有索引变体可往返；错配/损坏不替换当前索引 |
| [x] | 前端数据集与索引管理 | 上传、切换、保存、加载和管理员删除闭环 |
| [x] | 生命周期回归测试 | 重名、穿越、权限、重启、损坏、级联和原子性覆盖 |

### 模块 D：历史与高级可视化（P2，已完成）

| 完成 | 任务 | 验收 |
| --- | --- | --- |
| [x] | 查询记录落库与历史 API/UI | 用户隔离；管理员全局；原始向量不落库 |
| [x] | 评测记录落库与历史 API/UI | 保存请求参数和结果快照 |
| [x] | UMAP/PCA 二维投影 | 确定性抽样、缓存、查询点与 Top-K 强制补入 |
| [x] | 原生 SVG 高亮 | 查询点红色、结果蓝色、背景按类型着色 |
| [x] | 可视化直接查询 | 散点支持鼠标与键盘选择，并回填细胞编号执行查询 |

### 模块 E：正确性与工程交付（P0）

| 完成 | 任务 | 验收 |
| --- | --- | --- |
| [x] | L2/Cosine/IP 契约和严格输入校验 | 非法输入稳定返回 4xx，不污染活动状态 |
| [x] | 保证型 `cell_type` 条件检索 | 最坏情况下仍满足条件、数量和精确 Top-K 保证 |
| [x] | 线程安全和原子资源替换 | 构建/加载失败保留旧数据与索引 |
| [x] | 后端 pytest/Ruff，前端 Vitest/ESLint/build | 本地与 CI 使用相同质量门 |
| [x] | 后端直接依赖固定、前端 lockfile 固定 | Python/Node 环境可复现 |
| [x] | README、API、用户、测试和交付文档 | 描述与实现一致，内部链接有效 |
| [x] | 分阶段提交 | 每阶段独立 commit，正文含 Changes/Verification/Scope |

### 模块 F：`EXPERIMENT.md` 第 10 章加分方向

| 完成 | 方向 | 验收 |
| --- | --- | --- |
| [x] | 其他扩展功能 | 历史、UMAP/PCA 高亮和图上直接查询均有永久测试 |
| [x] | 多数据集联合检索 | 共享空间确认、联合指纹、来源映射、跨数据集结果与失效处理 |
| [x] | ANN 算法改进 | PQ 4×候选精确重排；Recall 不下降证明、固定提升回归、耗时与索引字节权衡 |
| [x] | RAG + 单细胞数据库 | 三类自然语言计划、真实证据、引用白名单、本地降级与可选 Responses provider |

## 三、代码外待办

以下项目需要项目组提供真实信息，不属于代码缺口：

- [ ] 组员、学号、角色与贡献度确认；
- [ ] 真实数据来源、许可、引用与脱敏说明；
- [ ] 最终远程仓库链接；
- [ ] 演示视频或现场演示安排；
- [ ] 若公开发布，权利人确认许可证；
- [ ] 目标评审机器上的截图和性能实测表。
